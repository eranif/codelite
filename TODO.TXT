Debug Adapter Client
---


- Update the Launch() method to accept environment variables
- Update the Dap Settings dialog to allow the user to choose the env format (LIST, DICTIONARY, NONE)
- Allow user to set function breakpoint from the UI
- Handle Module event and print it in the "Output" tab
- Add tooltip support
- Add "CanInteract" support to the vairous views (either by disabling the views, or by clearing them) + hide the debugger marker
- Write documentation for this plugin (with an example for how to configure lldb-vscode on MSW / Ubuntu)
- Settings dialog - remove the "scan" button and instead add a "help" button that opens the docs page
- Do the same for the LSP (add link to the docs page)
- Add support for file-system-workspace