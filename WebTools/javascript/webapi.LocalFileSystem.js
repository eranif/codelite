
/**
 * @brief The LocalFileSystem interface of the File System API gives you access to a sandboxed file system.  The methods are implemented by window and worker objects.
 * @link https://developer.mozilla.org/en-US/docs/Web/API/LocalFileSystem
 */
function LocalFileSystem() {

	/**
	 * @brief en/DOM/File_API/File_System_API/FileSystem
	 * @link https://developer.mozilla.org#const_temporary">TEMPORARY</a></code> or <code><a href="#const_persistent">PERSISTENT</a></code>.</dd>
 <dt>size</dt>
 <dd>The storage space—in bytes—that you need for your app.</dd>
 <dt>successCallback</dt>
 <dd>The success callback that is called when the browser provides a file system. Its argument is the <code><a href=
	 */
	this.ileSystem

}

