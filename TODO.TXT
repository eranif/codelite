`CTagsd` project

# TODO

1. `Add include file` should be moved to LSP via new event `wxEVT_LSP_FIND_HEADER`
2. `Outline Plugin` -> move to LSP
3. Fix code completion for header file names
4. All the content of the `Code Generation / Refactoring` should be moved to LSP
5. Remove parsing of files once saved (this is no longer needed)
6. Remove `symbols.db` creation (not needed anymore)
7. Navigation bar -> move to LSP
8. #include code completion
9. function calltip is not working after the first char is typed
