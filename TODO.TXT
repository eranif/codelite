- wxCrafter: make sure that after code generation an event is sent to git/svn views are updated accordingly
- CMakePlugin / wxCrafter: use icon for the tabs?
- Add events for all debugger UI actions so plugins code implement it instead of the default debugger
