//////////////////////////////////////////////////////////////////////
// This file was auto-generated by codelite's wxCrafter Plugin
// Do not modify this file by hand!
//////////////////////////////////////////////////////////////////////

#include "m_dataviewtemplatesmodel.h"

//////////////////////////////////////////
// Implementation
//////////////////////////////////////////

m_dataviewTemplatesModel::m_dataviewTemplatesModel()
    : m_colCount(0)
{
}

m_dataviewTemplatesModel::~m_dataviewTemplatesModel()
{
    for(size_t i=0; i<m_data.size(); ++i) {
        delete m_data.at(i);
    }
    m_data.clear();
}

unsigned int m_dataviewTemplatesModel::GetChildren(const wxDataViewItem& item, wxDataViewItemArray& children) const
{
    if(item.GetID() == NULL) {
        // Root
        for(size_t i=0; i<m_data.size(); ++i) {
            children.Add( wxDataViewItem( m_data.at(i) ) );
        }
        return children.size();
    }

    children.Clear();
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.m_pItem);
    if ( node ) {
        for(size_t i=0; i<node->GetChildren().size(); ++i) {
            children.Add( wxDataViewItem( node->GetChildren().at(i) ) );
        }
    }
    return children.GetCount();
}

unsigned int m_dataviewTemplatesModel::GetColumnCount() const
{
    return m_colCount;
}

wxString m_dataviewTemplatesModel::GetColumnType(unsigned int col) const
{
    if ( !m_data.empty() && m_data.at(0)->GetData().size() > col ) {
        return m_data.at(0)->GetData().at(col).GetType();
    }
    return "string";
}

wxDataViewItem m_dataviewTemplatesModel::GetParent(const wxDataViewItem& item) const
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.m_pItem);
    if ( node ) {
        return wxDataViewItem(node->GetParent());
    }
    return wxDataViewItem(NULL);
}

bool m_dataviewTemplatesModel::IsContainer(const wxDataViewItem& item) const
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.m_pItem);
    if ( node ) {
        return node->IsContainer();
    }
    return false;
}

void m_dataviewTemplatesModel::GetValue(wxVariant& variant, const wxDataViewItem& item, unsigned int col) const
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.m_pItem);
    if ( node && node->GetData().size() > col ) {
        variant = node->GetData().at(col);
    }
}
wxDataViewItem m_dataviewTemplatesModel::DoAppendItem(const wxDataViewItem& parent, const wxVector<wxVariant>& data, bool isContainer, wxClientData *clientData)
{
    m_dataviewTemplatesModel_Node* parentNode = reinterpret_cast<m_dataviewTemplatesModel_Node*>(parent.m_pItem);
    m_dataviewTemplatesModel_Node* child = new m_dataviewTemplatesModel_Node();
    child->SetIsContainer(isContainer);
    child->SetClientObject( clientData );
    child->SetData( data );
    if ( parentNode ) {
        parentNode->AddChild( child );

    } else {
        m_data.push_back( child );
    }

    return wxDataViewItem(child);
}

wxDataViewItem m_dataviewTemplatesModel::DoInsertItem(const wxDataViewItem& insertBeforeMe, const wxVector<wxVariant>& data, bool isContainer, wxClientData *clientData)
{
    m_dataviewTemplatesModel_Node* child = new m_dataviewTemplatesModel_Node();
    child->SetIsContainer(isContainer);
    child->SetClientObject( clientData );
    child->SetData( data );
    
    // find the location where to insert the new item
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(insertBeforeMe.m_pItem);
    if ( !node )
        return wxDataViewItem();
        
    wxVector<m_dataviewTemplatesModel_Node*>::iterator where = std::find(m_data.begin(), m_data.end(), node);
    
    if ( where !=  m_data.end() ) {
        // top level item
        m_data.insert( where, child );

    } else {
        
        if ( !node->GetParent() )
            return wxDataViewItem();
        
        child->SetParent(node->GetParent());
        where = std::find(node->GetParent()->GetChildren().begin(), node->GetParent()->GetChildren().end(), node);
        if ( where == node->GetParent()->GetChildren().end() ) {
            node->GetParent()->GetChildren().push_back( child );
            
        } else {
            node->GetParent()->GetChildren().insert(where, child);
            
        }
    }

    return wxDataViewItem(child);
}

wxDataViewItem m_dataviewTemplatesModel::AppendItem(const wxDataViewItem &parent, const wxVector<wxVariant>& data, wxClientData *clientData)
{
    wxDataViewItem ch = DoAppendItem(parent, data, false, clientData);
    ItemAdded(parent, ch);
    return ch;
}

wxDataViewItem m_dataviewTemplatesModel::AppendContainer(const wxDataViewItem &parent, const wxVector<wxVariant>& data, wxClientData *clientData)
{
    wxDataViewItem ch = DoAppendItem(parent, data, true, clientData);
    ItemAdded(parent, ch);
    return ch;
}

wxDataViewItemArray m_dataviewTemplatesModel::AppendItems(const wxDataViewItem &parent, const wxVector<wxVector<wxVariant> >& data)
{
    wxDataViewItemArray items;
    for(size_t i=0; i<data.size(); ++i) {
        items.push_back( DoAppendItem(parent, data.at(i), false, NULL) );
    }
    ItemsAdded(parent, items);
    return items;
}

bool m_dataviewTemplatesModel::SetValue(const wxVariant& variant, const wxDataViewItem& item, unsigned int col)
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.m_pItem);
    if ( node && node->GetData().size() > col ) {
        node->GetData().at(col) = variant;
    }
    return true;
}

void m_dataviewTemplatesModel::DeleteItem(const wxDataViewItem& item)
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.m_pItem);
    if ( node ) {
        wxDataViewItem parentItem(node->GetParent());
        ItemDeleted(parentItem, item);

        // this will also remove it from its model parent children list
        if ( node->GetParent() == NULL ) {
            // root item, remove it from the roots array
            wxVector<m_dataviewTemplatesModel_Node*>::iterator where = std::find(m_data.begin(), m_data.end(), node);
            if ( where != m_data.end() ) {
                m_data.erase(where);
            }
        }
        delete node;
    }
}

void m_dataviewTemplatesModel::DeleteItems(const wxDataViewItem& parent, const wxDataViewItemArray& items)
{
    // sanity
    for(size_t i=0; i<items.GetCount(); ++i) {
        m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(items.Item(i).m_pItem);
        wxUnusedVar(node);
        wxASSERT(node && node->GetParent() != parent.m_pItem);
        DeleteItem(items.Item(i));
    }
}

void m_dataviewTemplatesModel::Clear()
{
    wxVector<m_dataviewTemplatesModel_Node*> roots = m_data;
    wxVector<m_dataviewTemplatesModel_Node*>::iterator iter = roots.begin();
    for(; iter != roots.end(); ++iter) {
        DeleteItem( wxDataViewItem(*iter) );
    }
    Cleared();
}

bool m_dataviewTemplatesModel::IsEmpty() const
{
    return m_data.empty();
}

wxClientData* m_dataviewTemplatesModel::GetClientObject(const wxDataViewItem& item) const
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.GetID());
    if ( node ) {
        return node->GetClientObject();
    }
    return NULL;
}

void m_dataviewTemplatesModel::SetClientObject(const wxDataViewItem& item, wxClientData *data)
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.GetID());
    if ( node ) {
        node->SetClientObject(data);
    }
}

void m_dataviewTemplatesModel::UpdateItem(const wxDataViewItem& item, const wxVector<wxVariant>& data)
{
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.GetID());
    if ( node ) {
        node->SetData( data );
        ItemChanged( item );
    }
}

wxDataViewItem m_dataviewTemplatesModel::InsertItem(const wxDataViewItem& insertBeforeMe, const wxVector<wxVariant>& data, wxClientData *clientData)
{
    wxDataViewItem ch = DoInsertItem(insertBeforeMe, data, false, clientData);
    if ( ch.IsOk() ) {
        m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(ch.GetID());
        ItemAdded(wxDataViewItem(node->GetParent()), ch);
    }
    return ch;
}

wxDataViewItem m_dataviewTemplatesModel::InsertContainer(const wxDataViewItem& insertBeforeMe, const wxVector<wxVariant>& data, wxClientData *clientData)
{
    wxDataViewItem ch = DoInsertItem(insertBeforeMe, data, true, clientData);
    if ( ch.IsOk() ) {
        m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(ch.GetID());
        ItemAdded(wxDataViewItem(node->GetParent()), ch);
    }
    return ch;
}

wxVector<wxVariant> m_dataviewTemplatesModel::GetItemColumnsData(const wxDataViewItem& item) const
{
    if ( !item.IsOk() ) 
        return wxVector<wxVariant>();
        
    m_dataviewTemplatesModel_Node* node = reinterpret_cast<m_dataviewTemplatesModel_Node*>(item.GetID());
    if ( !node ) {
        return wxVector<wxVariant>();
    }
    return node->GetData();
}
