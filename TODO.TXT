Debug Adapter Client
---

- When scanning for python, add 2 debuggers:
    - debugpy | file
    - debugpy | module
- Allow user to set function breakpoint from the UI
- Update the docs on how to install `debugpy`: `pip install --upgrade debugpy`

others:
----

- fsw: move the "Tool chain" dropdown to the "Build" tab
- fsw: remove the "Debug" startup commands tab completely
- fsw: add interperter entry
- fsw: rename "Executable" to "File to run"
- fsw: rename: Code completion -> compile_flags.txt

Open Windows View:
---

- Use different colour to mark modified editors
