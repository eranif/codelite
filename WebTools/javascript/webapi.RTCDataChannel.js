
/**
 * @brief The RTCDataChannel interface represents a bi-directional data channel between two peers of a connection.
 * @link https://developer.mozilla.org/en-US/docs/Web/API/RTCDataChannel
 */
function RTCDataChannel() {

	/**
	 * @brief The read-only property RTCDataChannel.label returns a DOMString containing a name describing the data channel. There is no constraint of uniqueness about it.
	 * @link https://developer.mozilla.org/en-US/docs/Web/API/RTCDataChannel/label
	 */
	this.binaryType = '';

	/**
	 * @brief The read-only property RTCDataChannel.readyState returns an enum of the type RTCDataChannelState representing the state of the underlying data connection.
	 * @link https://developer.mozilla.org/en-US/docs/Web/API/RTCDataChannel/close" title='The RTCDataChannel.close() method closes the data channel. The closing is done in a non abrupt way. The state of the channel is set to "closing", the messages not yet sent are sent, then the channel is closed.'><code>RTCDataChannel.close()</code></a></dt>
 <dd>Closes the channel. The closing is done in a non abrupt way. The <a href=
	 */
	this.tate

}

