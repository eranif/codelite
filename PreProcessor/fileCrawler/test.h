#include <vector>
using  namespace   boost::filesystem;
#include <list>
using     namespace std;
#include <string>
#include <map>
#include <stdio.h>
#include <stdlib.h>

namespace bf = boost::filesystem ;


#define ERAN
#ifdef  ERAN
#define MY_CHECK(x, y) {\
    if(x < y){\
        printf();\
    }else{\
        printf();\
    }
    
#endif

