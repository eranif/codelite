var innerObj = (function(){
    notification : notification,
    createHTMLElement : createHTMLElement,
    getGetParameters : function(e) {
        var retObj = {
            open: function(title, content, buttons, hideCloseButton) {
                var popup = document.getElementById(popupId);
                },
            close: function() {
            }
        };
        retObj.